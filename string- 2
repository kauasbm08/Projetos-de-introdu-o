#include <stdio.h>
#include <string.h>

int main() {
    char nome[50];
    
    printf("Digite seu nome completo: ");
    scanf("%[^\n]", nome);
    
    nome[strcspn(nome, "\n")] = '\0';
    
    printf("Nome: %s\n", nome);
    
    return 0;
}
